const ThreadLine = () => {
  return (
    <div className="w-full flex justify-center py-16">
      <div className="relative w-px h-32 bg-gradient-to-b from-transparent via-gold to-transparent opacity-50 animate-fade-in" />
    </div>
  );
};

export default ThreadLine;
